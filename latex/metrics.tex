\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\usepackage{hyperref}
\usepackage{parskip}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{float}
\usepackage{graphicx}

\newcommand{\dnn}{\boldsymbol{\mathsf N}}
\newcommand{\dnnlayer}[1][i]{\dnn_{\boldsymbol{\hat\theta}^{(#1)}}}
\newcommand{\D}{\boldsymbol{\mathsf D}}
\makeatletter
\newcommand{\@varsup}[1]{\@ifnextchar\bgroup{\@varsup@arg{#1}}{#1}}
\newcommand{\@varsup@arg}[2]{#1^{(#2)}}
\newcommand{\residual}{\@varsup{\boldsymbol r}}
\newcommand{\x}{\@varsup{\boldsymbol x}}
\newcommand{\estimate}{\@varsup{\boldsymbol{\hat x}}}
\newcommand{\dirty}{\boldsymbol x_{\text{d}}}
\newcommand{\true}{\boldsymbol{x^{*}}}
\newcommand{\noise}{\boldsymbol{b}}
\makeatother

\title{Finding failure modes of R2D2 and CLEAN}
\author{Thomas Harper}
\date{\today}

\begin{document}

\maketitle
\newpage
\tableofcontents
\newpage

\section{Overview}

We search for failure modes by treating the observing setup as a parameter vector
\begin{equation}
\boldsymbol\theta =
(\rho_{\text{DR}}, t_{\text{obs}}, \nu_0, \Delta\nu, C, \ldots),
\end{equation}
\begin{itemize}
    \item $\boldsymbol\theta$ is the observing setup searched by nested sampling
    \item $\rho_{\text{DR}}$ is the target dynamic range
    \item $t_{\text{obs}}$ is the observation duration
    \item $\nu_0$ is the start frequency
    \item $\Delta\nu$ is the channel width
    \item $C$ is the telescope configuration
\end{itemize}
then we simulate data $\boldsymbol y(\boldsymbol\theta)$ using the MeqTrees software and attempt to reconstruct it with R2D2 \cite{aghabiglou2024r2d2} and CLEAN.
For an algorithm $A$, nested sampling targets a failure score
\begin{equation}
F_A(\boldsymbol\theta)
=
\mathcal M\left(\estimate_A(\boldsymbol y(\boldsymbol\theta)), \true(\boldsymbol\theta)\right),
\end{equation}
\begin{itemize}
    \item $A$ is the reconstruction algorithm, either R2D2 or CLEAN
    \item $F_A(\boldsymbol\theta)$ is the failure score for algorithm $A$ at setup $\boldsymbol\theta$
    \item $\boldsymbol y(\boldsymbol\theta)$ is the simulated visibility data
    \item $\estimate_A$ is the reconstruction produced by algorithm $A$
    \item $\true(\boldsymbol\theta)$ is the ground-truth sky (for the moment simply a point source)
    \item $\mathcal M$ is the chosen metric or composite failure score
\end{itemize}
where larger $F_A$ means worse reconstruction, higher residuals, or greater computational cost.
The high-scoring samples identify regions of observing conditions where R2D2, CLEAN, or both are likely to fail.

\subsection{Failure-Score Plots}

The images on the next page show the nested-sampling objective for the current R2D2 and CLEAN searches.
This is not a traditional likelihood or posterior: the quantity passed to nested sampling as the likelihood is the failure score $F_A$.
Bright regions therefore indicate parameter combinations with worse reconstructions, while dark regions indicate comparatively good performance.
White areas have the worst performance.
The merged plot shows combined failure structure, and the side-by-side plot separates algorithm-specific behaviour.

WSClean \cite{offringa2014wsclean} has been used for CLEAN, using default out of the box call time parameters without fine-tuning.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{../benchmarks/merged-r2d2-wsclean-likelihood.png}
\caption{Merged nested-sampling failure score for the R2D2 and CLEAN searches. The plotted objective is the failure score used in place of a traditional likelihood. White is the worst performance. Dark is the best performance.}
\label{fig:merged-r2d2-wsclean-likelihood}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{../benchmarks/merged-r2d2-wsclean-likelihood-side-by-side.png}
\caption{Side-by-side failure-score comparison for merged R2D2 and CLEAN nested-sampling runs. The comparison separates the algorithms so shared failure regions and algorithm-specific failure regions can be inspected directly. White is the worst performance. Dark is the best performance.}
\label{fig:merged-r2d2-wsclean-likelihood-side-by-side}
\end{figure}

\section{Failure Metrics}

The failure score $F_A(\boldsymbol\theta)$ is built from metrics that expose different kinds of reconstruction failure.
The currently defined metrics are:
\begin{itemize}
    \item \hyperref[sec:metric-snr]{Signal-to-noise ratio}: reconstruction error relative to the ground-truth image scale.
    \item \hyperref[sec:metric-log-snr]{Logarithmic signal-to-noise ratio}: the R2D2 logarithmic-intensity version of SNR.
    \item \hyperref[sec:metric-nmse]{Normalised mean squared error}: squared reconstruction error normalised by ground-truth image energy.
    \item \hyperref[sec:metric-normalised-residual]{Normalised residual}: final dirty-image residual relative to the observed dirty image.
\end{itemize}
For each nested-sampling run, $\mathcal M$ is chosen from these metrics or a composite of them, with signs chosen so larger $F_A$ corresponds to worse performance.

\section{Parameter Search}

Nested sampling searches over the following observing and simulation parameters.
The detailed definitions are given in the linked sections of the search parameter space.

\begin{table}[H]
\centering
\begin{tabular}{lll}
Dimension & PoC range & Meaning \\
\hline
\hyperref[sec:param-dynamic-range]{\texttt{dynamic\_range}} & $10^2$--$10^3$ & One-Jy source divided by thermal-noise sigma \\
\hyperref[sec:param-observation-duration]{\texttt{observation\_minutes}} & $4$--$10$ & Total requested observing time \\
\hyperref[sec:param-channel-count]{\texttt{channel\_count}} & $2$--$6$ & Number of frequency channels \\
\hyperref[sec:param-frequency-range]{\texttt{start\_frequency\_hz}} & $1.0\times10^9$--$1.1\times10^9$ & First channel frequency \\
\hyperref[sec:param-frequency-range]{\texttt{channel\_width\_hz}} & $0.5\times10^6$--$2.0\times10^6$ & Uniform spacing between channels \\
\end{tabular}
\caption{Nested-sampling parameter dimensions used in the current PoC runs.}
\label{tab:nested-sampling-parameters}
\end{table}

\section{Summary}

This document defines the candidate failure metrics and nested-sampling parameters for finding R2D2 and CLEAN failure modes.
The main question is whether these metrics capture the relevant reconstruction failures, and whether the proposed parameters and associated ranges are reasonable for the MeqTrees simulations.

The addendum contains the supporting data model, reconstruction equations, full metric definitions, and detailed parameter notes.

\appendix
\section*{Addendum}
\addcontentsline{toc}{section}{Addendum}

\section{Data Model}
\label{sec:data-model}

\begin{equation}
\boldsymbol y = \boldsymbol\Phi \true + \boldsymbol n
\end{equation}
\begin{itemize}
    \item $\boldsymbol y\in \mathbb C^M$ is the measured visibilities
    \item $\boldsymbol\Phi: \mathbb R^N \to \mathbb C^M$  is the measurement operator
    \item $\true\in\mathbb R_+^N$ is the true sky image
    \item $\boldsymbol n\in\mathbb C^M$ is Gaussian noise with s.d. $\tau>0$ and mean $0$
\end{itemize}

\begin{equation}
\dirty = \kappa\operatorname{Re}\{\boldsymbol\Phi^\dagger \boldsymbol y\} = \D\true + \noise
\end{equation}

\begin{itemize}
    \item $\dirty$ is the dirty image including noise
    \item $\kappa=\max(\operatorname{Re}{\boldsymbol\Phi^\dagger\boldsymbol\Phi\boldsymbol\delta})^{-1}>0$ is the normalisation factor ensuring the PSF ($\boldsymbol{h}=\kappa \operatorname{Re}\left\{\boldsymbol{\Phi}^{\dagger} \boldsymbol{\Phi} \boldsymbol{\delta}\right\} \in \mathbb{R}^N$) has peak value 1, where $\boldsymbol{\mathbf\delta} \in \mathbb R^N$ a discrete Dirac delta centred in the image (1 at the centre, 0 elsewhere)
    \item $\boldsymbol\Phi^\dagger$ is the adjoint of the measurement operator
    \item $\D:\mathbb R^N\to\mathbb R^N,\quad \D \triangleq \kappa\operatorname{Re}\{\boldsymbol{\Phi}^{\dagger}\boldsymbol{\Phi}\}$ is a normalised map for the true image to the noise free dirty image, encoding the non-uniform Fourier sampling, gridding, de-gridding and Fourier transform operations
    \item $\D\true$ is the noise-free dirty image
    \item $\noise = \kappa\operatorname{Re}\{\boldsymbol\Phi^\dagger\boldsymbol n\}\in\mathbf R^N$ is the normalised back projected noise
\end{itemize}

\section{Reconstruction Algorithms}

\subsection{R2D2}

\begin{gather}
    \residual{i} = \dirty - \D\x{i}\\
    \x{i} = \x{i-1} + \dnnlayer(\residual{i-1}, \x{i-1})
\end{gather}

\begin{itemize}
    \item $\residual{i}$ is a residual dirty image. It is the difference between the original noisy dirty image $\dirty$ and the one derived from the $i^\text{th}$ estimation of the night sky $\D\x{i}$
    \item $\x{i}$ is the $i^\text{th}$ estimate of the night sky produced by R2D2
    \item $\dnnlayer$ is the $i^\text{th}$ deep neural network layer in R2D2
    \item $\boldsymbol{\hat\theta}^{(i)}\in\mathbb R^Q$ are the learned parameters for a given DNN layer.
\end{itemize}

\begin{equation}
\estimate \triangleq \x{I} = \sum_{i=1}^I\dnnlayer(\residual{i-1}, \x{i-1})
\end{equation}

\begin{itemize}
    \item $\estimate$ is R2D2s final reconstruction
    \item $I$ is the total number of DNN layers
\end{itemize}

\subsection{Högbom CLEAN}

\begin{gather}
    \residual{i} = \dirty - \D\x{i}\\
    \x{i} = \x{i-1} + \gamma\,\residual{i-1}_{p^{(i)}}\,\boldsymbol{\delta}_{p^{(i)}}
\end{gather}

\begin{itemize}
    \item $\residual{i}$ is a residual dirty image. It is the difference between the original noisy dirty image $\dirty$ and the one derived from the $i^\text{th}$ CLEAN component model $\D\x{i}$
    \item $\x{i}$ is the $i^\text{th}$ CLEAN component model of the sky image, initialised at $\x{0}=\boldsymbol 0$
    \item $p^{(i)}=\arg\max_{n}\lvert\residual{i-1}_{n}\rvert$ is the pixel of the peak residual
    \item $\boldsymbol{\delta}_{p^{(i)}}$ is a discrete Dirac delta at pixel $p^{(i)}$
    \item $\gamma\in(0,1]$ is the loop gain
\end{itemize}

\begin{equation}
\x{I} = \sum_{i=1}^{I}\gamma\,\residual{i-1}_{p^{(i)}}\,\boldsymbol{\delta}_{p^{(i)}}
\end{equation}

\begin{equation}
\estimate \triangleq \boldsymbol g \ast \x{I} + \residual{I}
\end{equation}

\begin{itemize}
    \item $\x{I}$ is the CLEAN component model after $I$ iterations
    \item $\boldsymbol g$ is the restoring beam, typically a Gaussian fit to the main lobe of the PSF
    \item $\estimate$ is the restored CLEAN image
    \item $I$ is the number of CLEAN iterations
\end{itemize}

\section{Evaluation Metrics}

\subsection{Reconstruction Fidelity}

\subsubsection{Signal-to-Noise Ratio}
\label{sec:metric-snr}

\begin{equation}
    \mathrm{SNR}(\estimate, \true)
    =
    20\log_{10}
    \left(
    \frac{\|\true\|_2}
    {\|\true - \estimate\|_2}
    \right)
\end{equation}

\subsubsection{Logarithmic Signal-to-Noise Ratio}
\label{sec:metric-log-snr}

\paragraph{R2D2 definition}

\begin{gather}
    \operatorname{rlog}(\x)=x_{\max} \log _a\left(\frac{a}{x_{\max}} \x+\mathbf{1}\right)\\
    \operatorname{logSNR}(\estimate, \true)=\operatorname{SNR}(\operatorname{rlog}(\estimate), \operatorname{rlog}(\true))
\end{gather}

\begin{itemize}
    \item $a$ is a parameter set to $\rho_\text{DR}$, the dynamic range of the ground truth image
    \item $x_\text{max}$ is the peak pixel value of the image $\x$
    \item $\mathbf 1\in\mathbb R^N$ is a vector of values equal to 1
\end{itemize}


\subsubsection{Dynamic Range}

\subsubsection{Normalised Mean Squared Error}
\label{sec:metric-nmse}

\begin{equation}
    \operatorname{NMSE}(\estimate, \true)
    =
    \frac{\|\estimate - \true\|_2^2}
    {\|\true\|_2^2}
\end{equation}

\begin{itemize}
    \item $\estimate$ is the reconstructed image
    \item $\true$ is the ground-truth sky image
    \item $\|\estimate - \true\|_2^2$ is the squared reconstruction error
    \item $\|\true\|_2^2$ normalises the error by the ground-truth image energy
\end{itemize}

Lower NMSE indicates better reconstruction fidelity.

\subsection{Image-Domain Data Fidelity}

\subsubsection{Normalised Residual}
\label{sec:metric-normalised-residual}
$$
\bar\sigma_{\text{res.}}(\hat{\residual},\dirty)
=
\frac{\|\hat{\residual}\|_2}{\|\dirty\|_2}
$$

\begin{itemize}
\item $\hat\residual$ - final dirty-image residual
\item $\dirty$ - observed noisy dirty image
\end{itemize}

The normalized residual measures the magnitude of the final dirty-image residual relative to the magnitude of the original dirty image. Lower values therefore indicate greater data fidelity: the reconstructed sky model more closely reproduces the observed data.

For noisy observations, however, the ideal residual is not necessarily zero. The dirty image can be written as
$$
\dirty = \D\true + \noise,
$$
where $\noise$ is the image-domain noise. Therefore, even for a perfect reconstruction $\estimate=\true$,
$$
\hat\residual
=
\dirty-\D\estimate
=
\noise.
$$

The appropriate target is thus a residual consistent with the noise level rather than a residual of zero. Driving the residual substantially below the expected noise level may indicate that the reconstruction is fitting noise rather than recovering the underlying sky.

In short: how closely does each model reproduce the observed dirty image? Lower is better as a measure of data fidelity, but should be interpreted relative to the expected noise level.

% \subsection{Scientific Fidelity}

% \subsubsection{Flux Recovery}

% \subsubsection{Astrometric Error}

% \subsubsection{Source Detection and Completeness}

\section{Search Parameter Space}

\subsection{Sky and Image Parameters}

\subsubsection{Dynamic Range}
\label{sec:param-dynamic-range}

In the nested-sampling search, dynamic range is a simulation parameter rather than a metric computed from the output image.
MeqTrees predicts visibilities for a fixed point-source sky, then the simulator adds complex Gaussian thermal noise with
\begin{equation}
    \sigma_{\text{vis}}
    =
    \frac{S_{\text{src}}}{\rho_{\text{DR}}}.
\end{equation}

\begin{itemize}
    \item $\rho_{\text{DR}}$ is the requested simulation dynamic range
    \item $S_{\text{src}}$ is the true point-source flux, fixed to $1~\mathrm{Jy}$ in the current runs
    \item $\sigma_{\text{vis}}$ is the complex visibility noise standard deviation
\end{itemize}

Thus lower $\rho_{\text{DR}}$ produces noisier visibilities before R2D2 or CLEAN sees the data.
The R2D2 paper uses a dynamic range for ground truth images of $10^3\le \rho_\text{DR}\le5\times10^5$.

% \subsubsection{Source Morphology}

% \subsubsection{Source Position}

\subsection{Observation and Fourier-Sampling Parameters}

\subsubsection{VLA Configuration}
\label{sec:param-vla-configuration}

The simulator supports the four standard VLA configurations:
\begin{itemize}
    \item \texttt{vla.a}
    \item \texttt{vla.b}
    \item \texttt{vla.c}
    \item \texttt{vla.d}
\end{itemize}
These change the baseline distribution and therefore the Fourier coverage and dirty beam.
For the first PoC runs, we plan to fix this parameter to \texttt{vla.a} before expanding the search over configuration.

\subsubsection{Declination}
\label{sec:param-declination}

Suggested search range: $-15^\circ$--$75^\circ$.
The NRAO VLA Observational Status Summary states that its quoted array resolutions are appropriate for sources in this declination range \cite{nrao-vla-resolution}.
For the first PoC runs, declination can be fixed near the VLA latitude, $\delta \simeq 34^\circ$, before expanding the search.

\subsubsection{Observation Duration}
\label{sec:param-observation-duration}

VLA dynamic-scheduling timescales, EVLA Memo~232 \cite{faes2024evla232}:
\begin{itemize}
    \item Scan length: 0--10~min (Fig.~7)
    \item Single tracking (same target, consecutive scans): 0--10~min (Fig.~11)
    \item Time on source per scheduling block (even if interrupted): 0--20~min (Fig.~17)
\end{itemize}

For regular (standard observing) scans, no less than 20~seconds on source after slewing (Static Report Tab) \cite{nrao-vla-obsguide}.
Suggested search range: 20~s--20~min.

\subsubsection{Observation Start Time and Hour-Angle Coverage}

\subsubsection{Number of Frequency Channels}
\label{sec:param-channel-count}

The VLA can support channel counts of the form $2^n$ in relevant correlator modes, but practical limits depend on the observing mode and correlator setup.
For example, the OSS notes that phased-array pulsar subbands can be divided into an arbitrary $2^n$ number of channels \cite{nrao-vla-oss}.
For WIDAR spectral-line setups, Table~4.2.2 of the OSS gives example Baseline Board Pair configurations with spectral-channel counts ranging from 64 channels to 16384 channels, including examples at 2048 channels and above \cite{nrao-vla-oss}.
In practice, 64 channels is a common modest/default scale for setup exploration, while thousands of channels may be computationally infeasible for the present MeqTrees, R2D2, and CLEAN nested-sampling simulations.
Even 64 may computationally infeasible.

\subsubsection{Frequency Range and Bandwidth}
\label{sec:param-frequency-range}

Currently unsure which of these bands to use.
The channel width can then be a set to a maximum of of the full bandwidth available divided by the number of channels.
I am not sure what would make sense as a minimum channel width.

\begin{table}[H]
\centering
\begin{tabular}{ll}
4  & 54--86~MHz \\
P  & 224--480~MHz \\
L  & 1--2~GHz \\
S  & 2--4~GHz \\
C  & 4--8~GHz \\
X  & 8--12~GHz \\
Ku & 12--18~GHz \\
K  & 18--26.5~GHz \\
Ka & 26.5--40~GHz \\
Q  & 40--50~GHz \\
\end{tabular}
\caption{VLA receiver bands. Recommended calibrator cycle times by configuration are in Table~3.1 \cite{nrao-vla-obsguide}.}
\label{tab:vla-bands}
\end{table}

% \subsubsection{Flagging Fraction}

% \subsubsection{Visibility Weighting}

% \section{Proposed Search}

% \subsection{Search Variables}

% \subsection{Parameter Ranges}

% \subsection{Objective Metrics}

% \subsection{Failure Criteria}

\bibliographystyle{plain}
\bibliography{references}

\end{document}
